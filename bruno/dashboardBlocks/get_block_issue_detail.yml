info:
  name: GET Block Issue Detail
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/blocks/:blockId/issues/:issueId"
  params:
    - name: issueId
      value: "255"
      type: path
    - name: blockId
      value: t1lv940wmszrm6ketphh6rkc
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
