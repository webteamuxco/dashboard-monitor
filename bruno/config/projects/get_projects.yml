info:
  name: Get Projects
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/config/projects"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
