info:
  name: Get Pannels By projectId
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/config/projects/:projectId/panels?showDevelopmentPanel=true"
  params:
    - name: showDevelopmentPanel
      value: "true"
      type: query
    - name: projectId
      value: f20rkqy3w59m3k5kloosbwug
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
