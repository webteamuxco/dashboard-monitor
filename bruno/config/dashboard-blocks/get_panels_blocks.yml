info:
  name: GET Panels Blocks
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/config/dashboard-blocks"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
