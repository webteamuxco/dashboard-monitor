info:
  name: GET Panels KPI
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/config/dashboard-kpis"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
